# 🚀 Скрипт для запуска серверов разработки

Этот скрипт позволяет одновременно запустить все необходимые серверы для тестирования приложения.

## 📋 Что запускается

- **Next.js приложение** на порту `3001` (включает фронтенд и API)
- **Xero тест сервер** уже запущен на порту `3003`

## 🎯 Способы запуска

### Вариант 1: Через npm скрипт
```bash
npm run dev
# или
npm run start:all
```

### Вариант 2: Прямой запуск скрипта
```bash
./start-dev.sh
```

## 🛑 Остановка серверов

Для остановки всех серверов нажмите `Ctrl+C` в терминале где запущен скрипт.

## 📍 URL адреса

- **Next.js приложение**: http://localhost:3001
- **Xero тест**: http://localhost:3003

## ⚠️ Требования

- Node.js установлен
- Зависимости установлены в папке `backend/`
- Файл `.env` настроен в папке `backend/`

## 🔧 Устранение неполадок

1. **Порты заняты**: Убедитесь, что порт 3001 свободен
2. **Зависимости**: Выполните `npm install` в папке `backend/`
3. **Права доступа**: Убедитесь, что скрипт имеет права на выполнение (`chmod +x start-dev.sh`)